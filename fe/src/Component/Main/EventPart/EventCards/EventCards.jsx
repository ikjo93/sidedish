import styled from "styled-components";

const StyledEventCards = styled.div`
  border-top: solid #ebebeb 1px;
  border-bottom: solid #ebebeb 1px;
`;

const EventCards = () => {
  return <StyledEventCards>{}</StyledEventCards>;
};

export default EventCards;
